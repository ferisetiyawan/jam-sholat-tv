name: jam_sholat_tv
description: "Jam Sholat TV"

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  wakelock_plus: ^1.2.8
  dio: ^5.4.1
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  marquee: ^2.2.3
  audioplayers: ^6.5.1
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/background_masjid.jpeg
    - assets/sounds/beep_adzan.wav
    - assets/sounds/beep_iqomah.wav
